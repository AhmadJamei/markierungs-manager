تابع get_leaves دقیقاً چکار می‌کند؟

بی‌پرده:

این تابع فقط داده‌ها را از دیتابیس می‌خواند و به تقویم می‌دهد
👉 هیچ تغییری در دیتابیس ایجاد نمی‌کند

🧠 نقش آن در سیستم

در FullCalendar این خط:

events: "/calendar/get-leaves/"

یعنی:

تقویم میگه:
«برو از این آدرس داده‌ها رو بگیر و به من بده»

🔍 داخل get_leaves چه اتفاقی می‌افتد؟
leaves = Leave.objects.filter(user=request.user)

👉 همه روزهای ثبت‌شده این کاربر را می‌گیرد

بعد:

events.append({
    "title": leave.type,
    "start": leave.date.strftime("%Y-%m-%d"),
    "className": "selected-green" if leave.type == "ill" else "selected-blue"
})

👉 هر رکورد را تبدیل می‌کند به چیزی که FullCalendar بفهمد

🎯 خروجی نهایی

مثلاً این برمی‌گردد:

[
  {
    "title": "ill",
    "start": "2026-04-30",
    "className": "selected-green"
  },
  {
    "title": "holiday",
    "start": "2026-05-01",
    "className": "selected-blue"
  }
]
🧠 FullCalendar با این چکار می‌کند؟

👉 خودش:

روزها را پیدا می‌کند
کلاس را اعمال می‌کند
رنگ را نمایش می‌دهد
🔥 نکته خیلی مهم

این خط:

calendar.refetchEvents();

👉 دوباره get_leaves را صدا می‌زند

💥 ارتباط بین دو تابع
تابع	کار
toggle_leave	نوشتن در دیتابیس
get_leaves	خواندن از دیتابیس
🪓 جمع‌بندی بی‌رحمانه
get_leaves فقط reader است
هیچ logic پیچیده‌ای ندارد
کل UI تقویم به خروجی این وابسته است
🎯 نکته حرفه‌ای (خیلی مهم)

اگر طراحی درست باشه:

❗ تنها منبع حقیقت (source of truth) = get_leaves

نه چیزی که در JS رنگ می‌زنی


******************************************************
🎯 تابع toggle_leave دقیقاً چکار می‌کند؟

بی‌پرده:

هر بار که کاربر روی یک روز کلیک می‌کند
👉 این تابع فوراً در دیتابیس ایجاد یا تغییر انجام می‌دهد

🧠 خط‌به‌خط منطقش

نسخه‌ای که ساختیم:

leave, created = Leave.objects.get_or_create(
    user=request.user,
    date=date_obj,
    defaults={"type": leave_type}
)
این خط یعنی:

👉 برو ببین:

آیا برای این کاربر
در این تاریخ
رکوردی وجود دارد یا نه
🔍 دو حالت دارد
✅ حالت 1 — وجود ندارد

👉 رکورد جدید می‌سازد:

user=Ahmad, date=2026-04-30, type=ill
✅ حالت 2 — وجود دارد
if not created:
    leave.type = leave_type
    leave.save()

👉 فقط نوع را تغییر می‌دهد

مثلاً:

ill → holiday
💥 چرا اسمش toggle است؟

راستش؟
👉 اسمش یکم گمراه‌کننده است

چون:

toggle واقعی یعنی: روشن/خاموش
ولی اینجا داری:
create یا update انجام می‌دی

🧠 رفتار واقعی تابع
وضعیت قبلی	کلیک جدید	نتیجه
وجود ندارد	ill	ساخته می‌شود
ill	holiday	تغییر می‌کند
holiday	ill	تغییر می‌کند
🔴 مشکل اصلی این طراحی (که خودت فهمیدی)

❌ هر کلیک = عملیات دیتابیس

یعنی:

کاربر 10 بار کلیک کنه → 10 بار write
حتی اگر آخرش نظرش عوض شه

👉 این دقیقاً همون چیزیه که گفتی «درست نیست»

🎯 نتیجه‌گیری

تابع toggle_leave:

✔ کار می‌کند
✔ ساده است
❌ ولی برای UX و performance بد است

🪓 جمع‌بندی بی‌رحمانه
این تابع برای:
👉 prototype / تست سریع خوبه
ولی برای:
👉 سیستم واقعی طراحی ضعیفیه
🚀 قدم بعدی (که شروع کردیم)

ما داریم حرکت می‌کنیم به:

❗ به جای toggle_leave
👉 یک تابع مثل save_leaves (batch save)

اگر بخوای، قدم بعدی رو می‌تونیم دقیق طراحی کنیم:

👉 حذف کامل toggle
👉 جایگزینی با سیستم staging (pending changes)

اونجا دیگه وارد سطح واقعی backend design میشی.